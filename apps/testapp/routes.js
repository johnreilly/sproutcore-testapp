// Routes and statecharts seem a bit tricky.
// What we'd like is the two statechart states to 'live' at 
// different urls, so they're bookmarkable, etc.

// SC.routes.add('signin', ???)
// SC.routes.add('main', ???)